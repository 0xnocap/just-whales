{
  "name": "whale-town-marketplace",
  "version": "1.0.0",
  "description": "Whale Town NFT Marketplace contracts",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network tempo_testnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network tempo"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.1",
    "hardhat": "^2.28.6"
  }
}

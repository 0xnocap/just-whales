# Design Implementation Plan: StakingPage (Variant F Synthesis)

## Summary
- **Scope:** Component refactor of `StakingPage.tsx`
- **Winner variant:** Variant F (Synthesis)
- **Key improvements:** 
  - Centered, balanced layout optimized for mobile and desktop.
  - Tabbed interface to switch between "Stake NFTs" and "$OP Rewards".
  - "Ocean Emissions" branding with expressive typography and motion.
  - Horizontal scrollable NFT inventory with a "Browse All" action.
  - Consistent fixed height (650px) to prevent layout shifts.

## Files to Change
- [ ] `src/pages/StakingPage.tsx` - Complete rewrite to match Variant F.

## Implementation Steps

### 1. Structure & Layout
- Replace the current 2-column grid with a single centered card container (`max-w-2xl`).
- Implement the fixed-height container (`h-[650px]`) with `flex flex-col` and `overflow-hidden`.
- Add the branding header ("OCEAN EMISSIONS") above the main card.

### 2. Tab Switcher (The Pill Toggle)
- Use `useState` for `activeTab`.
- Create a pill-shaped background using `motion.div` for smooth transitions between tabs.
- Ensure border-radius consistency (2rem for card, custom rounded-xl for toggle).

### 3. "Stake NFTs" Tab
- **Stats:** Implement the 2-column stats grid for "Emission Rate" and "NFTs Staked".
- **Inventory:** Create the horizontal scrollable flex container.
- **Scrollbar:** Add the custom CSS for the sleek, low-profile horizontal scrollbar.
- **Action:** Add the "Initialize Staking" button with the "Awaiting Launch" hover state.

### 4. "$OP Rewards" Tab
- **Header:** Create the centered rewards display with large balance and "$OP" label.
- **Yield Stats:** 2-column grid for "Daily Yield" and "Life-time" rewards.
- **Staked Assets:** Implement the dashed-border placeholder for the empty vault state.
- **Actions:** Implement "Claim All $OP" and "Unstake" buttons.

### 5. Styling & Motion
- Use `motion/react` for tab transitions (X-axis slide) and initial entrance.
- Use Tailwind 4 theme tokens (e.g., `dream-cyan`, `dream-purple`, `ocean-deep`).
- Add the custom scrollbar CSS via a `<style>` tag or Tailwind utility.

## Accessibility Checklist
- [ ] Buttons have clear focus states.
- [ ] Tab buttons use proper ARIA roles (optional but recommended for production).
- [ ] Color contrast for $OP and Emission values meets AA standards.
- [ ] Navigation is keyboard-operable.

---
*Generated by Design Lab*
